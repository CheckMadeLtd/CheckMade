namespace CheckMade.Telegram.Tests.Unit;

// The separate bot menu definition file is needed for now because we set the bot command menus via BotFather by 
// copy and pasting from these .md files
// ToDo: quick spike of setting BotMenu dynamically from code / records, then we don't need these files and this test!

public class BotCommandMenuTests
{
    // ToDo: A test that checks equivalence of xyzBotCommandMenu records with bot_commands_master definition. 
    // Probably have seaprate md file for each BotType - and use RegEx to parse out command and descr?
    
    
}
